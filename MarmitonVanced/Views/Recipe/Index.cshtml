@using Models
@model List<Recipe>

<body class="text-center">
    <h1 class="display-4">Liste des recettes</h1>
    <div class="selectType">
        <button onclick="showTypeRecipe('Entree')">Entrée</button>
        <button onclick="showTypeRecipe('plat')">Plat</button>
        <button onclick="showTypeRecipe('dessert')">Dessert</button>
    </div>
    <div class="list" id="Entree">
        @foreach (Recipe recipe in Model.Where(recipe => recipe.Type == RecipeType.Entrée))
        {
            <button class="recipe" onclick="location.href ='/recipe/details?id=@recipe.Id'">
                <img src="@recipe.Image" alt="Recette" />
                <div>
                    <div>
                        <p>@recipe.Name</p>
                        <p>Temps : @recipe.Time</p>
                    </div>
                    <p>@recipe.CountRecipe</p>
                </div>
            </button>
        }
    </div>
    <div class="list" id="plat" style="visibility:hidden">
        @foreach (Recipe recipe in Model.Where(recipe => recipe.Type == RecipeType.Plat))
        {
            <button class="recipe" onclick="location.href ='/recipe/details?id=@recipe.Id'">
                <img src="@recipe.Image" alt="Recette" />
                <div>
                    <div>
                        <p>@recipe.Name</p>
                        <p>Temps : @recipe.Time</p>
                    </div>
                    <p>@recipe.CountRecipe</p>
                </div>
            </button>
        }
    </div>
    <div class="list" id="dessert" style="visibility:hidden">
        @foreach (Recipe recipe in Model.Where(recipe => recipe.Type == RecipeType.Dessert))
        {
            <button class="recipe" onclick="location.href ='/recipe/details?id=@recipe.Id'">
                <img src="@recipe.Image" alt="Recette" />
                <div>
                    <div>
                        <p>@recipe.Name</p>
                        <p>Temps : @recipe.Time</p>
                    </div>
                    <p>@recipe.CountRecipe</p>
                </div>
            </button>
        }
    </div>
</body>
<button onclick="location.href ='recipe/create'" class="addNew">
    <p>Création/Modification de recette</p>
</button>
